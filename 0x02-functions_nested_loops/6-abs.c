#include "main.h"

/**
 * _abs - check the absolute number of an integer
 * @r: integer to be checked
 *
 * Return: the absolute value of 'r'
 */

int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (-r);
	}
}
