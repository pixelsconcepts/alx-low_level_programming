#include "main.h"

/**
 * _isupper - check if c is uppercare or lowercase
 * @c: Character to be checked
 * Return: 1 if uppercase and 0 if otherwise
 */

int _isupper(int c)
{

if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
